[package]
name = "bharat-boot-diagnostics"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Boot diagnostics overlay for BharatOS"
[dependencies]
bharat-libsurface = { path = "../../libs/libsurface" }
bharat-libcore = { path = "../../libs/libcore" }
